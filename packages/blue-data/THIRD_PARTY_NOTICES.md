# Third-party notices

The BlueX7 modern renderer incorporates the six lookup tables listed in `resources/blue-x7-modern/ATTRIBUTION.md` from Google Mobile Synthesis FM (msfa), Copyright 2012 Google Inc. The tables are under Apache License 2.0. See `LICENSES/Apache-2.0.txt` for the full license and `ATTRIBUTION.md` for the exact source files and table names.

Blue-authored `@blue/data` code is MIT. The package distribution includes both grants and declares `MIT AND Apache-2.0` in its package metadata.

The package also declares `@rgrove/parse-xml` 4.2.3 (ISC) and `quickjs-emscripten` 0.32.0 (MIT) as runtime dependencies. npm installs those packages separately with their own license files. The app dependency inventory records these workspace-package dependencies and the QuickJS support packages.
