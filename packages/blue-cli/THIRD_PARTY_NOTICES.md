# Third-party notices

The Blue CLI bundle includes code from `@blue/data`, which incorporates Google msfa lookup tables under Apache-2.0. It also bundles `@rgrove/parse-xml` 4.2.3 under ISC. See `LICENSES/Apache-2.0.txt`, `LICENSES/ISC.txt`, and `THIRD_PARTY_NOTICES.md` in `@blue/data` for license texts and attribution details.

The CLI's Blue-authored code is MIT. The packed CLI declares `MIT AND Apache-2.0 AND ISC`. Its runtime dependency `quickjs-emscripten` 0.32.0 and the associated QuickJS support packages are MIT and installed separately by npm.
