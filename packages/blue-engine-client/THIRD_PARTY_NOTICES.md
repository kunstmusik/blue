# Third-party notices

Blue's engine client source is MIT. Its `zeromq` 6.8.0 runtime dependency declares `MIT AND MPL-2.0`; its upstream package carries the corresponding terms. This dependency is installed separately and is not copied into the client package's source distribution.
